# ── Optional — Server URL ─────────────────────────────────────────────────────
# Defaults to http://localhost:3001/api in development.
# Set this when deploying the client to Vercel/Netlify and the server to Render/Railway.
# Example: VITE_API_URL=https://codeviz-api.onrender.com/api
VITE_API_URL=
